name: simple_otp
description: A simple OTP library to implement OTP text fields in flutter.
version: 0.0.1
homepage: https://github.com/noahweasley/Simple-OTP

environment:
  sdk: ">=3.1.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

# The following section is specific to Flutter packages.
flutter:
